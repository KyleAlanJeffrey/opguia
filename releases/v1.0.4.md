# v1.0.4

Restore native window and pin Python version.

- Restored pywebview as a required dependency (native window by default)
- Pinned `requires-python` to `>=3.10,<3.14` so pip fails fast on unsupported Python versions instead of failing mid-build on pythonnet
